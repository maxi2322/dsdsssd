# Kopiere diese Datei nach .env und trage deinen API-Key ein.
# .env wird beim Start automatisch geladen (python-dotenv) und NICHT eingecheckt.

ANTHROPIC_API_KEY=sk-ant-...
# Nur nötig, wenn llm.provider in config.yaml auf "openai" gesetzt wird:
# OPENAI_API_KEY=sk-...
